{
  "name": "ezmonitor",
  "private": true,
  "version": "0.1.0",
  "description": "Parent/child home network screen monitoring suite",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@ezmonitor/shared && npm run build --workspace=@ezmonitor/parent && npm run build --workspace=@ezmonitor/child",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "start:parent": "npm run start --workspace=@ezmonitor/parent",
    "start:child": "npm run start --workspace=@ezmonitor/child"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
